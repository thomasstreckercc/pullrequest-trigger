This plugin allows an administrator to define for each Bitbucket repository to launch a specific plan branch whenever a pull request is created or modified.
Once the pull request is merged or rejected, the plan branch will be disabled and should be cleaned up with the default Bamboo processed.